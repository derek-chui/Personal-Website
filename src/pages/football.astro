---
import Layout from '../layouts/Layout.astro';
---

<style>
    /* General CSS */

    /* Root */
    :root {
        --apple-blue: #2997ff;
        --bg-dark: #000;
        --card-bg: #1a1a1a;
    }

    /* Key Frames */
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* Base Styles */
    body {
        background-color: var(--bg-dark);
        color: white;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        line-height: 1.6;
        margin: 0;
        padding: 0;
        overflow-x: hidden;
    }

    /* Typography */
    h1, h2, h3 {
        font-weight: 600;
        margin-bottom: 20px;
    }

    h2 {
        font-size: 2.2rem;
        position: relative;
        display: inline-block;
    }

    h2:after {
        content: '';
        position: absolute;
        width: 50%;
        height: 4px;
        bottom: -8px;
        left: 0;
        background: linear-gradient(90deg, var(--apple-blue), transparent);
        border-radius: 2px;
    }

    h3 {
        font-size: 1.6rem;
    }

    /* Links & Buttons */
    a {
        color: var(--apple-blue);
        text-decoration: none;
        transition: all 0.3s ease;
        position: relative;
    }

    a:hover {
        color: var(--apple-blue);
    }

    a::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: -2px;
        left: 0;
        background-color: var(--apple-blue);
        transition: width 0.3s ease;
    }

    a:hover::after {
        width: 100%;
        display: none;
    }

    /* Cards */
    html {
        scroll-behavior: smooth;
    }

    .card {
        background-color: var(--card-bg);
        border-radius: 16px;
        padding: 28px;
        margin-bottom: 28px;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
        opacity: 0;
        animation: fadeIn 0.6s forwards;
        animation-delay: calc(var(--order) * 0.1s);
    }

    .card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 28px rgba(0, 0, 0, 0.25);
        border-color: rgba(255, 255, 255, 0.2);
    }

    section {
        margin-bottom: 60px;
        opacity: 0;
        transform: translateY(20px);
        transition: all 0.6s ease;
    }

    section.visible {
        opacity: 1;
        transform: translateY(0);
    }

    /* Gallery */
    .gallery-wrapper {
        position: relative;
        margin-top: 24px;
    }

    .gallery-scroll {
        display: flex;
        overflow-x: auto;
        gap: 20px;
        padding: 16px 0 8px 4px;
        scroll-snap-type: x mandatory;
        scrollbar-width: thin;
        scrollbar-color: rgba(255,255,255,0.2) transparent;
        -webkit-overflow-scrolling: touch;
    }

    .gallery-scroll::-webkit-scrollbar { height: 6px; }
    .gallery-scroll::-webkit-scrollbar-track { background: transparent; }
    .gallery-scroll::-webkit-scrollbar-thumb {
        background-color: rgba(255, 255, 255, 0.2);
        border-radius: 3px;
    }

    .gallery-item {
        flex: 0 0 360px;
        width: 360px;
        flex-shrink: 0;
        scroll-snap-align: start;
        background: var(--card-bg);
        border-radius: 16px;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .gallery-item:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
    }

    .gallery-item img {
        width: 100%;
        height: 220px;
        object-fit: contain;
        display: block;
        background: #000;
    }

    .gallery-caption {
        padding: 14px 16px;
        color: rgba(255, 255, 255, 0.85);
        font-size: 0.95rem;
    }

    @media (max-width: 768px) {
        .gallery-item { min-width: 280px; }
        .gallery-item img { height: 180px; }
    }

    /* Lightbox */
    .gallery-item img,
    .gallery-media img {
        cursor: zoom-in;
    }

    .lightbox {
        position: fixed;
        inset: 0;
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.2s ease;
    }

    .lightbox.open {
        opacity: 1;
        pointer-events: auto;
    }

    .lightbox-backdrop {
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.75);
    }

    .lightbox-content {
        position: relative;
        z-index: 1;
        width: min(1000px, 92vw);
        max-height: 86vh;
        padding: 14px;
        border-radius: 16px;
        background: var(--card-bg);
        border: 1px solid rgba(255,255,255,0.12);
        box-shadow: 0 18px 60px rgba(0,0,0,0.6);
        transform: translateY(10px) scale(0.98);
        transition: transform 0.2s ease;
    }

    .lightbox.open .lightbox-content {
        transform: translateY(0) scale(1);
    }

    .lightbox-img {
        width: 100%;
        max-height: 72vh;
        object-fit: contain;
        display: block;
        border-radius: 12px;
    }

    .lightbox-caption {
        margin-top: 10px;
        color: rgba(255,255,255,0.8);
        font-size: 0.95rem;
    }

    .lightbox-close {
        position: absolute;
        top: 8px;
        right: 10px;
        width: 38px;
        height: 38px;
        border-radius: 10px;
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(0,0,0,0.35);
        color: white;
        cursor: pointer;
        font-size: 22px;
        line-height: 1;
        z-index: 10;
    }

    @media (max-width: 768px) {
        .lightbox-content {
            padding-top: calc(56px + env(safe-area-inset-top));
        }

        .lightbox-close {
            top: calc(10px + env(safe-area-inset-top));
            right: 12px;
        }
    }

    /* Responsive Adjustments */
    @media (max-width: 768px) {
        h2 {
            font-size: 1.8rem;
        }
    }

    /* Featured App Section */
    .featured-app {
        display: flex;
        gap: 32px;
        align-items: center;
        padding: 32px;
        background: var(--card-bg);
        border-radius: 20px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        margin-bottom: 40px;
        transition: all 0.3s ease;
    }

    .featured-app:hover {
        transform: translateY(-4px);
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
        border-color: rgba(255, 255, 255, 0.15);
    }

    .featured-app-image {
        flex: 0 0 420px;
        border-radius: 14px;
        overflow: hidden;
        background: #000;
    }

    .featured-app-image img {
        width: 100%;
        height: 280px;
        object-fit: contain;
        display: block;
        cursor: zoom-in;
    }

    .featured-app-content {
        flex: 1;
    }

    .featured-app-content h2 {
        font-size: 2rem;
        margin-bottom: 16px;
    }

    .featured-app-content p {
        color: rgba(255, 255, 255, 0.75);
        font-size: 1.05rem;
        line-height: 1.7;
        margin-bottom: 20px;
    }

    .featured-app-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 12px 24px;
        background: var(--apple-blue);
        color: white;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .featured-app-link:hover {
        background: #1a7fe8;
        transform: translateY(-2px);
    }

    .featured-app-link::after {
        display: none;
    }

    @media (max-width: 900px) {
        .featured-app {
            flex-direction: column;
            text-align: center;
        }

        .featured-app-image {
            flex: none;
            width: 100%;
            max-width: 420px;
        }

        .featured-app-content h2:after {
            left: 50%;
            transform: translateX(-50%);
        }
    }

    /* Story Flow Sections */
    .story-section {
        text-align: center;
        padding: 60px 20px;
        position: relative;
    }

    .story-section.intro {
        padding-top: 40px;
        padding-bottom: 0;
    }

    .story-section h1 {
        font-size: 2.8rem;
        margin-bottom: 16px;
        background: linear-gradient(135deg, #fff 0%, rgba(255,255,255,0.8) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .story-section p {
        font-size: 1.25rem;
        color: rgba(255, 255, 255, 0.75);
        max-width: 700px;
        margin: 0 auto;
        line-height: 1.7;
    }

    .story-section a {
        font-weight: 600;
    }

    .story-section .step-label {
        display: inline-block;
        font-size: 0.75rem;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--apple-blue);
        margin-bottom: 12px;
        opacity: 0.9;
    }

    /* Tools Section */
    .tools-section {
        padding: 12px 20px 0;
        text-align: center;
    }

    .tools-section .step-label {
        display: inline-block;
        font-size: 0.75rem;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--apple-blue);
        margin-bottom: 12px;
        opacity: 0.9;
    }

    .tools-section p {
        font-size: 1.1rem;
        color: rgba(255, 255, 255, 0.6);
        margin: 12px auto 0;
        white-space: nowrap;
    }

    .tool-badges {
        display: flex;
        flex-wrap: nowrap;
        justify-content: center;
        gap: 12px;
        margin: 0 auto;
    }

    .tool-badge {
        padding: 10px 20px;
        background: rgba(255, 255, 255, 0.06);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 24px;
        font-size: 0.95rem;
        color: rgba(255, 255, 255, 0.85);
        transition: all 0.3s ease;
    }

    .tool-badge:hover {
        background: rgba(41, 151, 255, 0.15);
        border-color: rgba(41, 151, 255, 0.3);
        transform: translateY(-2px);
    }

    /* Result Section */
    .result-section {
        text-align: center;
        padding: 50px 20px 30px;
    }

    .result-section .step-label {
        display: inline-block;
        font-size: 0.75rem;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--apple-blue);
        margin-bottom: 12px;
        opacity: 0.9;
    }

    .result-section p {
        font-size: 1.15rem;
        color: rgba(255, 255, 255, 0.6);
        max-width: 600px;
        margin: 0 auto 30px;
        line-height: 1.6;
    }

    @media (max-width: 768px) {
        .story-section h1 {
            font-size: 2rem;
        }
        .story-section p {
            font-size: 1.1rem;
        }
        .tool-badge {
            padding: 8px 14px;
            font-size: 0.85rem;
        }
        .tools-section,
        .result-section {
            padding: 40px 20px;
        }
    }

    /* Featured App Title Link */
    .featured-app-title {
        color: white;
        text-decoration: none;
    }

    .featured-app-title:hover {
        color: var(--apple-blue);
    }

    .featured-app-title::after {
        display: none;
    }

    /* Suite Labels */
    .suite-label {
        color: var(--apple-blue);
        font-weight: 600;
    }

    /* Wolves Twitter Section */
    #wolves-twitter .card {
        background-color: #1a1a1a;
        border-left: 6px solid #fdb913;
    }

    #wolves-twitter h2:after {
        background: linear-gradient(90deg, #fdb913, transparent);
    }

    #wolves-twitter h3 {
        color: #fdb913;
    }

    #wolves-twitter .card:hover {
        box-shadow: 0 12px 28px rgba(253, 185, 19, 0.2);
        border-color: rgba(253, 185, 19, 0.3);
    }

    #wolves-twitter img {
        transition: transform 0.3s ease, opacity 0.3s ease;
    }

    #wolves-twitter img:hover {
        transform: scale(1.05);
        opacity: 1;
    }

    #wolves-twitter a {
        color: #fdb913;
    }

    #wolves-twitter a:hover {
        color: #ffe066;
    }

    #wolves-twitter a::after {
        background-color: #fdb913;
    }

    /* Industry Connections Section */
    .connections-section {
        text-align: center;
        padding: 24px 20px 20px;
    }

    .connections-section .step-label {
        display: inline-block;
        font-size: 0.75rem;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--apple-blue);
        margin-bottom: 12px;
        opacity: 0.9;
    }

    .connections-section p {
        font-size: 1.15rem;
        color: rgba(255, 255, 255, 0.7);
        max-width: 700px;
        margin: 0 auto 32px;
        line-height: 1.7;
    }

    .league-logos {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 48px;
        flex-wrap: wrap;
        margin-top: 24px;
    }

    .league-logo {
        height: 60px;
        width: auto;
        opacity: 0.85;
        transition: all 0.3s ease;
        filter: grayscale(20%);
    }

    .league-logo:hover {
        opacity: 1;
        transform: scale(1.08);
        filter: grayscale(0%);
    }

    @media (max-width: 768px) {
        .league-logos {
            gap: 32px;
        }
        .league-logo {
            height: 48px;
        }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const sections = document.querySelectorAll('section');

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });

        sections.forEach(section => {
            observer.observe(section);
        });

        const lightbox = document.getElementById('lightbox') as HTMLDivElement | null;
        const lbImg = lightbox?.querySelector('.lightbox-img') as HTMLImageElement | null;
        const lbCaption = lightbox?.querySelector('.lightbox-caption') as HTMLElement | null;

        let activeHref: string | null = null;
        let armNavigateAt = 0;

        const openLightbox = (imgEl: HTMLImageElement) => {
            if (!lightbox || !lbImg || !lbCaption) return;

            lbImg.src = imgEl.src;
            lbImg.alt = imgEl.alt || '';
            lbCaption.textContent =
                imgEl.dataset.caption ||
                (imgEl.closest('.gallery-item')?.querySelector('.gallery-caption') as HTMLElement | null)?.textContent ||
                '';

            activeHref = imgEl.dataset.link || null;
            armNavigateAt = Date.now() + 400;

            lightbox.classList.add('open');
            lightbox.setAttribute('aria-hidden', 'false');
            document.body.style.overflow = 'hidden';
        };

        const closeLightbox = () => {
            if (!lightbox || !lbImg) return;

            lightbox.classList.remove('open');
            lightbox.setAttribute('aria-hidden', 'true');
            document.body.style.overflow = '';

            activeHref = null;

            setTimeout(() => { lbImg.src = ''; }, 150);
        };

        document
            .querySelectorAll<HTMLImageElement>('.gallery-item img, .gallery-media img')
            .forEach((img) => img.addEventListener('click', () => openLightbox(img)));

        lbImg?.addEventListener('click', (e) => {
            if (!lightbox?.classList.contains('open')) return;
            if (!activeHref) return;
            if (Date.now() < armNavigateAt) return;

            e.preventDefault();
            e.stopPropagation();
            const w = window.open(activeHref, '_blank', 'noopener,noreferrer');
            if (w) w.opener = null;
        });

        lightbox?.addEventListener('click', (e) => {
            const target = e.target;
            if (!(target instanceof Element)) return;

            if (target.matches('[data-lightbox-close]')) {
                e.preventDefault();
                e.stopPropagation();
                closeLightbox();
            }
        });

        document.addEventListener('keydown', (e: KeyboardEvent) => {
            if (e.key === 'Escape' && lightbox?.classList.contains('open')) closeLightbox();
        });
    });
</script>

<Layout title="Football Analytics">

    <!-- STEP 1: THE ROLE -->
    <section class="story-section intro visible">
        <h1>Football Analytics</h1>
        <p>
            I support both the coaching staff and athletic performance with data<br>at Santa Clara Broncos <a href="https://santaclarabroncos.com/sports/womens-soccer" target="_blank" rel="noopener noreferrer">Women's</a> and <a href="https://santaclarabroncos.com/sports/mens-soccer" target="_blank" rel="noopener noreferrer">Men's</a> Soccer.
        </p>
    </section>

    <!-- STEP 2: THE TOOLS -->
    <section class="tools-section visible">
        <span class="step-label">Tools</span>
        <div class="tool-badges">
            <span class="tool-badge">Hudl Wyscout</span>
            <span class="tool-badge">Catapult</span>
            <span class="tool-badge">VALD</span>
            <span class="tool-badge">Output</span>
            <span class="tool-badge">1080</span>
            <span class="tool-badge">LedsReact</span>
        </div>
        <p>In turn I built matchdays.app to centralize the analytics workflows and tie all these tools together.</p>
    </section>

    <!-- FEATURED APP CARD -->
    <section id="matchdays" style="--order: 0; margin-top: -30px;">
        <div class="featured-app">
            <div class="featured-app-image">
                <img src="/fa0.png" loading="lazy" data-caption="Click the image for project details." data-link="https://matchdays.app/about" />
            </div>
            <div class="featured-app-content">
                <h2><a href="https://matchdays.app" target="_blank" rel="noopener noreferrer" class="featured-app-title">matchdays.app</a></h2>
                <p>
                    This platform is two-fold:
                </p>
                <p>
                    <span class="suite-label">Coaching Suite:</span> Turns Wyscout PDFs into interactive dashboards, compares players side by side, and breaks down zone stats.
                </p>
                <p>
                    <span class="suite-label">Performance Center:</span> Connects to Catapult, VALD, Output, 1080, and Ledsreact APIs to automate report generation and track athlete metrics over time.
                </p>
                <p>
                    Currently used across <a href="https://santaclarabroncos.com/" target="_blank" rel="noopener noreferrer">Santa Clara Broncos Athletics</a> including Women's Soccer, Men's Soccer, Men's Basketball,
                    Women's Basketball, Women's Volleyball, and more soon.
                </p>
                <a href="https://matchdays.app/about" target="_blank" rel="noopener noreferrer" class="featured-app-link">
                    Learn More →
                </a>
            </div>
        </div>
    </section>

    <!-- INDUSTRY CONNECTIONS -->
    <section class="connections-section visible">
        <span class="step-label">Connections</span>
        <p>
            Building these tools has connected me with coaching staff, performance analysts, and sports scientists working professionally across the football industry.
            <br>I regularly talk with professionals at teams in the MLS, USL, EFL, and NCAA Division I.
        </p>
        <div class="league-logos">
            <img src="/mls.svg" alt="MLS" class="league-logo" />
            <img src="/usl.png" alt="USL" class="league-logo" />
            <img src="/efl.svg" alt="EFL" class="league-logo" />
            <img src="/ncaa.avif" alt="NCAA" class="league-logo" />
        </div>
    </section>

    <!-- GALLERY -->
    <section id="gallery-fa" style="--order: 1;">
    <div class="card">
        <h2>Previous Projects</h2>
        <p>Work that eventually became the building blocks of matchdays.app:</p>

        <div class="gallery-wrapper">
        <div class="gallery-scroll">

            <div class="gallery-item">
            <img src="/fa00.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md/#hudl-wyscout-match-report-extension" />
            <div class="gallery-caption">Hudl Wyscout Match Report Extension</div>
            </div>

            <div class="gallery-item">
            <img src="/fa1.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md/#player-performance-outliers" />
            <div class="gallery-caption">Player Catapult & VALD Outliers</div>
            </div>

            <div class="gallery-item">
            <img src="/fa11.png" loading="lazy" data-caption="Click the image for project details." data-link="https://plscout.streamlit.app/" />
            <div class="gallery-caption">Premier League Scouting Portal</div>
            </div>

            <div class="gallery-item">
            <img src="/fa2.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md#2---2025-summer-squad-assessment" />
            <div class="gallery-caption">Transfer List Comparison</div>
            </div>

            <div class="gallery-item">
            <img src="/fa3.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md#match-reviews" />
            <div class="gallery-caption">Game Shot Preceding Actions</div>
            </div>

            <div class="gallery-item">
            <img src="/fa4.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md#3---player-actions-v-usage-rates" />
            <div class="gallery-caption">Goal Contributions vs Usage Rate</div>
            </div>

            <div class="gallery-item">
            <img src="/fa5.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md#player-dashboard" />
            <div class="gallery-caption">Player Dashboard</div>
            </div>

            <div class="gallery-item">
            <img src="/fa6.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md#case-studies" />
            <div class="gallery-caption">Player Phase Involvement Comparison</div>
            </div>

            <div class="gallery-item">
            <img src="/fa7.png" loading="lazy" data-caption="Click the image for project details." data-link="https://github.com/derek-chui/Football-Analytics/blob/main/README.md#1---progression-analysis-2425" />
            <div class="gallery-caption">Progression Analysis</div>
            </div>
        </div>
        </div>
    </div>
    </section>

    <!-- WOLVES TWITTER SECTION -->
    <section id="wolves-twitter" style="--order: 2;">
        <div class="card">
            <h2>Twitter (X)</h2>
            <div style="display: flex; align-items: center; gap: 24px;">
                <div style="flex: 1;">
                    <h3>Wolves Data Analytics</h3>
                    <p>I also post data analytics about <a href="https://www.wolves.co.uk/" target="_blank" rel="noopener noreferrer">Wolverhampton Wanderers</a> on <a href="https://x.com/thatderekchui" target="_blank" rel="noopener noreferrer">Twitter (X)</a>.<br>Using a lot of my existing workflows as shown above to do so.</p>
                </div>
                <div style="flex: 0 0 100px;">
                    <img src="/wolves.png" alt="Wolves Logo" style="width: 100%; opacity: 0.9;">
                </div>
            </div>
        </div>
    </section>

    <!-- LIGHTBOX -->
    <div id="lightbox" class="lightbox" aria-hidden="true">
    <div class="lightbox-backdrop" data-lightbox-close></div>

    <figure class="lightbox-content" role="dialog" aria-modal="true" aria-label="Expanded image">
        <button class="lightbox-close" type="button" data-lightbox-close aria-label="Close">×</button>
            <img class="lightbox-img" alt="" />
        <figcaption class="lightbox-caption"></figcaption>
    </figure>
    </div>

</Layout>
